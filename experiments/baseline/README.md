# Baseline Experiment

This directory stores reproducible retrieval metrics for the local sample corpus.

Generate results:

```bash
PYTHONPATH=src python scripts/run_experiment.py
```

Outputs:

- `metrics.csv`
- `metrics.json`

